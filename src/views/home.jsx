const React = require('react')
const Default = require('./Default')

const Home = () => {
    return (
        <Default>
            <h1>
                Welcome To the Market!
            </h1>
        </Default>
    )
}

export default Home